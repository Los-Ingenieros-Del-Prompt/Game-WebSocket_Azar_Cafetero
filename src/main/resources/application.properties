spring.application.name=game
server.port=8085

# WebSocket Configuration
spring.websocket.server.address=0.0.0.0
logging.level.root=INFO
logging.level.org.springframework.web.socket=DEBUG
logging.level.org.springframework.messaging=DEBUG

# Lobby Service URL
lobby.service.url=http://localhost:8082

# Optional fallback floor id for Brisca floor events when floorId is not provided in requests
game.floor.brisca-id=
